# sketchlib-go

## Design Goals
1. Completed sketch library that can be used by pre-compute engine (standalone or streaming engine version), storage engine, and inference engine.
2. supporting sketch optimizations, such as NitroSketch, OctoSketch
3. supporting sketch frameworks, such as Exponential Histogram
